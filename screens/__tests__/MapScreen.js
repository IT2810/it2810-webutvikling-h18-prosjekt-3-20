import 'react-native';

describe('<MapScreen/>', () => {
  it('should render properly', async () => {
    // const tree = renderer.create(<MapScreen/>);
    // expect(tree).toMatchSnapshot();
  });
});
